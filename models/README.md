# CarbonSense Research Models

`models/trained/` contains the user-supplied research artifacts. These files are preserved for reproducibility and comparison.

They are not silently substituted for the frozen model runtime used by the web application. A new training artifact requires independent evaluation, contract validation, and explicit deployment approval.
