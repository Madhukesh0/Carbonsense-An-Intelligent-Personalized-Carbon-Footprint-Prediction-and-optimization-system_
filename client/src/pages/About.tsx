import { Card } from "@/components/ui/card";
import RepoShell from "@/components/RepoShell";
import { CircleHelp, ShieldCheck, Sparkles } from "lucide-react";

function DocMetric({ icon: Icon, label, value, detail, tone = "green" }: { icon: typeof Sparkles; label: string; value: string; detail: string; tone?: "green" | "navy" }) {
  return <Card className="border-0 bg-white p-5 shadow-[0_8px_30px_rgba(20,67,50,.06)]"><div className="flex items-start justify-between"><span className={`grid h-10 w-10 place-items-center rounded-2xl ${tone === "navy" ? "bg-[#e9eef8] text-[#315582]" : "bg-[#e5f7ec] text-[#157f54]"}`}><Icon size={19} /></span><span className="text-xs font-semibold uppercase tracking-[.16em] text-slate-400">{label}</span></div><p className="mt-5 text-3xl font-semibold tracking-tight text-[#102a2d]">{value}</p><p className="mt-1 text-sm text-slate-500">{detail}</p></Card>;
}

export default function About() {
  return <RepoShell><main className="mx-auto max-w-5xl px-4 py-16 lg:px-8"><span className="text-sm font-semibold uppercase tracking-[.18em] text-[#157f54]">Docs</span><h1 className="mt-3 text-4xl font-extrabold tracking-tight text-slate-900 sm:text-5xl dark:text-white">Documentation: methods and data boundaries.</h1><p className="mt-5 max-w-3xl text-lg leading-8 text-slate-500">CarbonSense pairs a model-based estimate with a transparent baseline computed from the same cited factor set. The ML target is formula-derived and synthetic, so it is not a verified measurement of personal emissions.</p><div className="mt-10 grid gap-5 md:grid-cols-3"><DocMetric icon={Sparkles} label="Model lens" value="15 → 36" detail="XGBoost v2.5 feature contract" /><DocMetric icon={CircleHelp} label="Baseline" value="v2.5-aligned" detail="Shared cited factors, fully auditable" tone="navy" /><DocMetric icon={ShieldCheck} label="Privacy" value="Scoped" detail="User-owned history" /></div><Card className="mt-6 border-0 bg-white p-7 shadow-sm"><h2 className="text-2xl font-bold tracking-tight">What the result means</h2><p className="mt-4 leading-7 text-slate-600">The deployed <strong>XGBoost</strong> model transforms your 15 questionnaire answers into a 36-feature vector: six activity numerics, numeric country factors (the Ember 2025 grid factor in kgCO₂e/kWh and the per-currency grocery factor), household size, and drop-first one-hot encodings of travel, vehicle fuel, heating, diet, waste, recycling, and cooking. Its ranked contribution view (TreeSHAP) shows which modeled input groups moved that result most relative to its base value; it is a comparison aid, not a causal environmental-impact claim.</p><p className="mt-4 text-base text-slate-500">The transparent baseline is the deterministic form of the same cited-factor construction the model was trained on — Ember 2025 country grid factors (India 0.67013, US 0.38440, UK 0.21741 kgCO₂e/kWh, with declared renewable-supply proxies), IPCC 2006 fuel chemistry at mid-range fleet economy, Scarborough 2023 diet bands, OWID air-travel bands, EEA clothing, and the IPCC waste convention with recycling credits — with household per-person attribution on shared lines. Because both paths share one factor set, any gap between the AI estimate and the baseline reflects model behaviour, not factor drift. Each estimate shows its line-by-line arithmetic; diet, air-frequency, waste-bag, and clothing lines remain clearly labelled screening proxies.</p></Card><Card className="mt-6 border-0 bg-[#eef8f2] p-7 shadow-sm"><p className="text-sm font-semibold uppercase tracking-[.16em] text-[#157f54]">Current runtime and frozen contract</p><h2 className="mt-3 text-2xl font-bold tracking-tight">What is live today</h2><p className="mt-4 max-w-3xl leading-7 text-slate-600">The active CarbonSense server executes the persisted <code className="mx-1 rounded bg-white px-1.5 py-0.5 text-sm">final-xgboost-v25</code> artifact on the <code className="mx-1 rounded bg-white px-1.5 py-0.5 text-sm">carbonsense-synthetic-v2.5</code> dataset (India/US/UK grid factors, household per-person attribution). Artifact hashes are verified before loading, and the displayed range is a 90% split-conformal interval (±71.3 kg, 91.4% empirical coverage).</p><p className="mt-4 max-w-3xl leading-7 text-slate-600">Recommendations are re-scored after every submission: each approved action is estimated with the shared factor set from your latest answers, ranked by planning impact, and a goal slider builds a plan toward a chosen reduction target. The What-if simulator recomputes both methods for a lifestyle scenario you select.</p></Card><Card className="mt-6 border-0 bg-white p-7 shadow-sm"><p className="text-sm font-semibold uppercase tracking-[.16em] text-[#157f54]">Verified model scope</p><h2 className="mt-3 text-2xl font-bold tracking-tight">One trained footprint predictor</h2><p className="mt-4 max-w-3xl leading-7 text-slate-600">The live predictor is one persisted <strong>XGBoost</strong> model, version <code className="mx-1 rounded bg-slate-100 px-1.5 py-0.5 text-sm">final-xgboost-v25</code>. Its recorded held-out evaluation uses an 8,000 / 2,000 split and reports R² 0.9514, MAE 32.2 kgCO₂e, and RMSE 44.6 kgCO₂e, with a train/test gap of +3.1% and 5-fold CV 0.9506 ± 0.0051. These metrics measure agreement with the formula-derived synthetic target, not direct measurement of a person’s emissions.</p><p className="mt-4 max-w-3xl leading-7 text-slate-600">The transparent baseline is a separate country-aware factor calculation, not a second trained model, and it is never averaged with the XGBoost result. Contribution groups explain how the model moved a particular estimate relative to its base value. The personal activity forecast runs Prophet once your recorded ledger reaches 12 consecutive completed months and 90+ distinct activity days (ETS from 6 months, and a labelled directional fallback before that) — it is a planning aid, not a verified prediction. All planning outputs are decision-support calculations; none are additional trained regressors.</p></Card></main></RepoShell>;
}
